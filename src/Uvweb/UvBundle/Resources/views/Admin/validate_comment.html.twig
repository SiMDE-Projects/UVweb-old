<div class="modal-header">
  <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
  <h3>Valider un avis</h3>
</div>

<form data-async data-target="#comment-list #comment-{{ comment.id }}" method="POST" action="{{ path('uvweb_admin_comment_validate', { 'commentid': comment.id }) }}">

  <div class="modal-body">
      <p>Valider l'avis suivant ?</p>
      <div style="width: 90%;">
          {% include 'UvwebUvBundle:Uv:comment_summary_row.html.twig' with {'comment': comment} %}
      </div>
  </div>

  <div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Annuler</button>
      <a><button class="btn btn-success">Valider</button></a>
  </div>

</form>
