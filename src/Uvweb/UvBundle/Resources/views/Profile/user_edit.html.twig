{% extends "UvwebUvBundle::layout.html.twig" %}

{% form_theme edit_user_form 'UvwebUvBundle:Form:bootstrap_row.html.twig' %}

{% block title %}
    Édition du profil - {{ parent() }}
{% endblock %}

{% block body %}

    <div class="page-header">
        <h2>Édition du profil</h2>
    </div>

    <div class="row-fluid">
        <div class="well">
                {{ form_errors(edit_user_form) }}
            <form class="form-horizontal" method="post" {{ form_enctype(edit_user_form) }}>
                {{ form_widget(edit_user_form) }}
                <div class="control-group">
                  <div class="controls">
                    <button type="submit" class="btn btn-success">Valider</button>
                  </div>
                </div>
                {{ form_rest(edit_user_form) }}
            </form>
        </div>
    </div>

{% endblock %}